name: Pascal Chess

on:
  pull_request:
  push:
    branches: [master]

permissions:
  contents: read

concurrency:
  group: chess-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        runner: [ubuntu-24.04, ubuntu-24.04-arm]
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - name: Compile and test rules, search and both interfaces
        run: make test
      - name: Verify the terminal package
        run: make image smoke
      - name: Validate Compose and whitespace
        run: |
          docker compose config --quiet
          git diff --check
